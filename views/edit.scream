{% extends "layout.scream" %}

{% block content %}
<div class="container py-4 px-3 mx-auto">
    <section class="page-shell">
        <h1 class="page-title mb-3">{{ pageTitle }}</h1>

        <form action="{{ action }}" method="POST">
            <input type="hidden" name="_method" value="PATCH">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                {% if errors.title %}
                <input type="text" name="title" class="form-control is-invalid" id="title" value="{{ fields.title }}" aria-describedby="title">
                <div class="invalid-feedback d-block">{{ errors.title }}</div>
                {% else %}
                <input type="text" name="title" class="form-control" id="title" value="{{ fields.title }}" aria-describedby="title">
                {% endif %}
            </div>

            <div class="mb-3">
                <label for="description" class="form-label">Description</label>
                <textarea id="description" name="description" class="form-control" rows="3">{{ fields.description }}</textarea>
            </div>

            <div class="row g-3">
                <div class="col-md-4">
                    <label for="priority" class="form-label">Priority</label>
                    <select id="priority" name="priority" class="form-select">
                        <option value="low" {% if fields.isLowPriority %}selected{% endif %}>Low</option>
                        <option value="medium" {% if fields.isMediumPriority %}selected{% endif %}>Medium</option>
                        <option value="high" {% if fields.isHighPriority %}selected{% endif %}>High</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label for="statusCode" class="form-label">Status</label>
                    <select id="statusCode" name="statusCode" class="form-select">
                        <option value="open" {% if fields.isOpen %}selected{% endif %}>Open</option>
                        <option value="completed" {% if fields.isCompleted %}selected{% endif %}>Completed</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label for="dueAt" class="form-label">Due Date</label>
                    {% if errors.dueAt %}
                    <input type="date" id="dueAt" name="dueAt" class="form-control is-invalid" value="{{ fields.dueAt }}">
                    <div class="invalid-feedback d-block">{{ errors.dueAt }}</div>
                    {% else %}
                    <input type="date" id="dueAt" name="dueAt" class="form-control" value="{{ fields.dueAt }}">
                    {% endif %}
                </div>
            </div>

            <div class="d-flex gap-2 flex-wrap mt-3">
                <button type="submit" class="btn btn-primary">{{ submitLabel }}</button>
                <a href="/todos/{{ todoId }}" class="btn btn-outline-secondary">Back</a>
            </div>
        </form>

        <div class="d-flex gap-2 flex-wrap mt-3">
            <form action="/todos/{{ todoId }}/toggle" method="POST" class="d-inline">
                <button type="submit" class="btn btn-outline-secondary">Toggle Status</button>
            </form>
            <form action="/todos/{{ todoId }}" method="POST" class="d-inline">
                <input type="hidden" name="_method" value="DELETE">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
        </div>
    </section>
</div>

{% endblock content %}
