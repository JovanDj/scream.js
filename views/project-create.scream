{% extends "layout.scream" %}

{% block content %}
<div class="container py-4 px-3 mx-auto">
    <section class="page-shell">
        <h1 class="page-title mb-3">Create Project</h1>
        <form action="/projects" method="POST">
            <div class="mb-3">
                <label class="form-label" for="name">Name</label>
                {% if errors.name %}
                <input id="name" name="name" type="text" class="form-control is-invalid" value="{{ fields.name }}">
                <div class="invalid-feedback d-block">{{ errors.name }}</div>
                {% else %}
                <input id="name" name="name" type="text" class="form-control" value="{{ fields.name }}">
                {% endif %}
            </div>
            <div class="d-flex gap-2">
                <button class="btn btn-primary" type="submit">Create</button>
                <a href="/projects" class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </section>
</div>
{% endblock content %}
